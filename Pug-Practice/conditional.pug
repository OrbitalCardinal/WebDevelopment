doctype html
html
    head
        title Conditionals
    body
        h2 My web page
        - let user = {name: "Domenic", loggedIn: false, lastLogin:11};
        if user.loggedIn
            p 
                | Welcome back,
                strong  #{user.name}
        else if user.lastLogin < 10
            p  Your last loggin was #{user.lastLogin} minutes ago
            p  
                a(href="/login") Log In again
        else 
            a(href="/login") Log In